# Variables d'environnement pour la production Render
# Copier ce fichier et remplacer les valeurs par les vraies

# Base de données PostgreSQL (fournie par Render)
DATABASE_URL="postgresql://username:password@hostname:5432/database"

# Sécurité - Générer une clé forte en production
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"

# Configuration de l'application
NODE_ENV="production"
PORT=3000

# CORS - URL du frontend déployé
FRONTEND_URL="https://your-frontend-domain.com"

# Pour générer une clé JWT sécurisée :
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
